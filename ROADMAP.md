# 产品路线图

本路线图用于记录 Markdown Reading Room 的产品方向，不代表固定排期或交付承诺。版本范围、实现顺序和技术方案会根据用户测试、平台限制、维护成本与实际优先级调整。

## 产品方向

项目将从以 Agent Skill 为主的部署工具，逐步拓展为本地优先的跨平台 Markdown 文档产品。网页端、Windows 和 macOS 是当前主线；Android 与 iOS 在桌面端基础稳定后推进。现有 Skill 交付方式进入兼容维护状态，不再作为主要演进方向。

## 方向性版本规划

- **v0.4.2：产品化基础改造。** 建立共用前端、平台适配接口与 Tauri 桌面工程。
- **v0.4.3：桌面原生能力。** 支持 Windows/macOS 原生目录选择、自动发现和实时监听。
- **v0.4.4：共享核心。** 引入 Rust 文档扫描核心、本地索引与全文搜索，逐步移除 Python 运行时依赖。
- **v0.4.5：桌面交付。** 生成 Windows 安装包和 macOS 应用包。
- **v0.4.6～v0.4.9：质量与发布。** 完善自动更新、性能、崩溃恢复、签名及跨平台测试。
- **v0.5.0：移动端基础。** 优先发布 Android APK/AAB，随后推进 iOS、TestFlight 与 App Store。

## 调整原则

每个版本以可测试、可回退和不破坏本地文档安全为前提。若用户验证暴露出更高优先级的问题，可以调整顺序、拆分范围或延后版本，不为追赶版本号牺牲稳定性。
